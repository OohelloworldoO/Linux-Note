Ctrl+z 放在終止並背景執行

jobs 可以列出背景工作狀態，編號

fg + [編號] 丟回前景執行

bg + [編號] 背景繼續執行

nice -n +一個正數或負數 +指令 =指定指令優先權值[PRI(預設80) + NI(-20~19)]

sudo !! =以超級使用者執行上一個指令

kill pid =溫和的中指
kill -9 pid =暴力終止
pkill +name =停止多個

/proc 行程資訊目錄
